import styled from "styled-components";

const Overview = () => {
  return (
    <Wrapper>
      <h2>Overview</h2>
    </Wrapper>
  );
};
export default Overview;

const Wrapper = styled.div`
  color: var(--black);
`;
