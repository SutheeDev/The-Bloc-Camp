import styled from "styled-components";

const Profile = () => {
  return (
    <Wrapper>
      <h2>Profile</h2>
    </Wrapper>
  );
};
export default Profile;

const Wrapper = styled.div`
  color: var(--black);
`;
