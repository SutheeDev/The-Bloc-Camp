import styled from "styled-components";

const CreateShow = () => {
  return (
    <Wrapper>
      <h2>Create Shows</h2>
    </Wrapper>
  );
};
export default CreateShow;

const Wrapper = styled.div`
  color: var(--black);
`;
